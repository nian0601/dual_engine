#version 330 core

out vec4 FragColor;

in vec4 Position;
in vec4 NormalAndRoughness;
in vec4 ColorAndMetalness;

void main()
{
   FragColor = vec4(abs(NormalAndRoughness.xyz), 1.0);
   //FragColor = vec4(1.0, 1.0, 1.0, 1.0);
};
