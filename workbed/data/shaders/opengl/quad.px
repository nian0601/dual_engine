#version 330 core

out vec4 FragColor;

in vec2 texCoord;

uniform sampler2D AlbedoTexture;

void main()
{
   FragColor = texture(AlbedoTexture, texCoord);
   FragColor.g = FragColor.r;
   FragColor.b = FragColor.r;
   FragColor.w = FragColor.r;
};
